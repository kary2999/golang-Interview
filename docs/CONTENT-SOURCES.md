<!-- [skill: go-team-standards · 内容来源文档 · dev-dna] 记录题库来源、提取和补充策略 -->
# 题库内容来源

## 本地来源

- 本地文件名：`2025Go面试八股（含100道答案） - 知乎.html`
- 页面标题：`2025Go面试八股（含100道答案） - 知乎`
- 原始知乎地址：<https://zhuanlan.zhihu.com/p/1913871969356583824>

原始下载页面及其资源目录不属于本项目，也不得复制进项目。构建脚本只读取项目上级目录中的本地 HTML，并生成经过清理的题库数据。

## 提取与清理规则

`scripts/extract_questions.py` 只处理文章正文 `Post-RichText` 区域中以数字开头的 `h3` 标题。数字标题作为问题，标题之后到下一数字标题之间的正文作为答案；非数字 `h3` 的内容并入前一道题，遇到正文后的 `h2` 时停止。

提取器只保留 `p`、`ul`、`ol`、`li`、`pre`、`code`、`strong`、`b`、`em`、`i`、`blockquote` 和 `br`。除格式合法的 `code` 语言类名外，所有属性都会移除；链接只保留可见文字。脚本、样式、SVG、表单及其子内容不会进入题库。

合并完成后，全部 100 条记录再次通过 `validate_questions` 的严格边界检查，验证题号、非空内容、允许标签、属性和标签嵌套。浏览器数据写入 `data/questions.js`，不包含远程资源引用或可执行 HTML。

## 原始内容异常

本地存档实际提取出 99 个数字题目，存在以下三处明确异常：

- ID 73：原文中没有该题号。
- ID 89：保留原文问题“如何优化内存使用？”，但原文答案为空。
- ID 90：保留原文问题“如何优化垃圾回收？”，但原文答案为空。

## 补充策略与来源标签

为满足 1–100 共 100 道题且每题有答案的产品契约：

- ID 73 补充“Go 中值接收者和指针接收者有什么区别？”及对应答案。
- ID 89、90 保留原文问题，只补充技术答案。
- ID 73、89、90 统一标记为 `source: "supplemented"`，答案开头明确显示“补充整理”。
- 其余 97 条记录统一标记为 `source: "zhihu-archive"`。

除上述三条补充记录外，导入答案保留文章措辞，只进行了结构提取和安全清理，并未逐条完成事实核查；页面中的内容应视为面试复习参考，而非官方或标准答案。

## 重新生成

在项目根目录运行：

```bash
python3 scripts/build_questions.py "../2025Go面试八股（含100道答案） - 知乎.html" data/questions.js
```
